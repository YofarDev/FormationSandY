package c4


fun main() {

    // Afficher une addition de deux nombres aléatoires à l'utilisateur
    // Si il trouve la bonne réponse, il gagne 1 point, on lui affiche son score
    // et on passe à une nouvelle addition.
    // Si il a faux, on lui signale et on lui repose la même addition.
    // Le jeu s'arrête à 3 points.
    // Un message "Fin du jeu" quand il est terminé.

    // Pour générer des nombres aléatoires :
    // val number1 = Random.nextInt(1, 10)
    // val number2 = Random.nextInt(1, 100)

    var points = 0
    var wrongAnswer = true

    do {

        // ?

        do {

            // ?

        } while (wrongAnswer)

    } while (points < 3)

}