package c10


fun main() {

    // Pour chaque nombre de la liste ci-dessous, imprimer si le nombre est pair ou impair
    // Pour cela utiliser deux fonctions :
    // Une fonction top-level (placée dans un fichier "utils") qui
    // retourne "true" si le nombre est pair
    // Une fonction placée au même endroit que main() qui imprime
    // le nombre suivi de son type (pair ou impair)
    // (le paramètre de cette fonction ne sera pas une liste mais un nombre)

    val numbers = listOf(4, 8, 15, 16, 23, 42)

    println(numbers)
}
